{{!-- <div class="container mt-4">
    <h2>My Bookings</h2>
    
    <button id="flightButo" class="btn btn-primary mb-3">View My Bookings</button>

    <!-- Modal -->
    <div class="modal fade" id="flightModal" tabindex="-1" aria-labelledby="flightModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="flightModalLabel">My Bookings</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div id="booking-list"></div>
                    <div id="booking-details" style="display: none;">
                        <button id="backToList" class="btn btn-secondary mb-3">Back to List</button>
                        <div id="details-content"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> --}}